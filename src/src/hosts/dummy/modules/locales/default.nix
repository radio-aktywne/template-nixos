# Enable locales
{
  i18n = {
    supportedLocales = [
      # Simple locale
      "C.UTF-8/UTF-8"

      # English
      "en_US.UTF-8/UTF-8"
    ];

    # Use English as the default locale
    defaultLocale = "en_US.UTF-8";
  };
}
